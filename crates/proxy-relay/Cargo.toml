[package]
name = "proxy-relay"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
proxy-common = { path = "../proxy-common" }
proxy-session = { path = "../proxy-session" }
proxy-store = { path = "../proxy-store" }

axum = { version = "0.7", features = ["ws"] }
bytes = "1"
chrono = "0.4"
dashmap = "6"
futures = "0.3"
http = "1"
http-body-util = "0.1"
hyper = "1"
hyper-util = "0.1"
reqwest = { version = "0.12", default-features = false, features = ["stream", "rustls-tls", "rustls-tls-webpki-roots", "json", "socks"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
tracing = "0.1"
